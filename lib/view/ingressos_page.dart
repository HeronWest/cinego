import 'package:flutter/material.dart';

class IngressosPage extends StatelessWidget {
  const IngressosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Ingressos'),
    );
  }
}
